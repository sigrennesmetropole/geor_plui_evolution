kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: pluievolution-back-qua-rm-pluievolution-back
  namespace: rennes-metropole-georchestra
  labels:
    app.kubernetes.io/instance: pluievolution-back-qua
    app.kubernetes.io/name: pluievolution-back-qua
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'true'
    nginx.ingress.kubernetes.io/force-ssl-redirect: 'true'
    nginx.ingress.kubernetes.io/rewrite-target: /pluievolution/$2
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
spec:
  tls:
    - hosts:
        - >-
          pluievolution-back-qua.rennes-metropole-georchestra.karbon.open.global
      secretName: pluievolution-back-qua.rennes-metropole-georchestra.karbon.open.global
  rules:
    - host: pluievolution-back-qua.rennes-metropole-georchestra.karbon.open.global
      http:
        paths:
          - path: /pluievolution(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: pluievolution-back-qua-rm-pluievolution-back
                port:
                  number: 8080

